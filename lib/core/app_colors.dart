import 'package:flutter/material.dart';

class AppColors {
  // Cores de Fundo
  static const Color background = Color(0xFF0F0F0F); // Preto quase absoluto
  static const Color surface = Color(
    0xFF1E1E1E,
  ); // Cinza bem escuro (Inputs/Barras)
  static const Color card = Color(0xFF252525); // Cinza do Card

  // Cor Principal (A Marca)
  static const Color primary = Colors.deepPurpleAccent;

  // Status
  static const Color open = Colors.green;
  static const Color closed = Colors.red;
  static const Color star = Colors.amber;

  // Textos
  static const Color textWhite = Colors.white;
  static const Color textGrey = Colors.grey;
}
